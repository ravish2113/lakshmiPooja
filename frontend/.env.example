# Local Vite does not need this because vite.config.js proxies /api to localhost:8080.
# For Netlify, set this in Site configuration -> Environment variables.
VITE_API_BASE_URL=https://YOUR-KOYEB-APP.koyeb.app/api
